To use, run the following commands (linux).

First CD into the ICR folder. Then run:
```
unzip icr-identify-age-related-conditions.zip -d data
```
Then run:
```
python ./DataProcessing/datapipe.py
```
Then run:
```
python ./trainPip.py
```